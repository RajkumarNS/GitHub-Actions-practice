name: My first workflow
on: push
job:
    first_job:
      runs_on: ubuntu-latest
      steps:
      - name: welcome message
        run: echo:My first GitHub Actions Job"

      - name: List files
        run: ls

      -name: Read files
        run: cat README.md
        
